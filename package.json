Ensure package.json includes:
"dependencies": {
  "@xorng/template-base": "^1.0.0",
  "@xorng/template-validator": "^1.0.0"
}
(Adjust versions as needed based on project requirements)
  "name": "@xorng/validator-security",
  "version": "0.1.0",
  "description": "XORNG Security validation sub-agent",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src/",
    "test": "vitest"
  },
  "dependencies": {
    "@xorng/template-validator": "file:../template-validator",
    "@xorng/template-base": "file:../template-base",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pino": "^8.17.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "tsx": "^4.6.2",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
